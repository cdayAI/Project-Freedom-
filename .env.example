# Copy to .env (gitignored) and fill in. NEVER commit real keys.
# Paper account only in this repo — live keys should not be given to the
# system at all; live order flow is doubly locked regardless (see
# alpha_forge/execution/broker.py).
ALPACA_BASE_URL=https://paper-api.alpaca.markets
ALPACA_API_KEY_ID=
ALPACA_API_SECRET_KEY=

# Optional data vendors (unlock survivorship-free equities / options / futures)
# SHARADAR_API_KEY=
# POLYGON_API_KEY=
# ORATS_API_KEY=
# DATABENTO_API_KEY=
